package org.ThefryGuy.techFactory.config;

/**
 * Type enumeration for configuration values
 * Used by ConfigKey to enforce type safety
 */
public enum ConfigType {
    LONG,
    INT,
    DOUBLE,
    BOOLEAN,
    STRING
}

